let XBase = require('XBase');

cc.Class({
    extends: XBase,

    properties: {
    },

    Init(){
        this.InitData();
    },

    Execute(eventCode, message){
        switch (eventCode) {
            case 1:
                break;
            default:
                break;
        }
    },

    InitData(){

    },

});
