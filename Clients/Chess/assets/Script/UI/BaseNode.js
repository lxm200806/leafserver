let XBase = require('XBase');

cc.Class({
    extends: XBase,

    properties: {
    },

    XFInit(){
        this.InitData();
    },

    Execute(eventCode, message){
        switch (eventCode) {
            case 1:
                break;
            default:
                break;
        }
    },

    InitData(){

    },

});
